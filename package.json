{
  "name": "commonplace",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build:data": "python3 scripts/build-moods.py",
    "prepare:pages": "npm run check && node scripts/prepare-pages.js",
    "check": "node scripts/check-static.js",
    "start": "python3 -m http.server 8765 --bind 127.0.0.1 --directory dist"
  }
}
